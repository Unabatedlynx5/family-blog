# Environment Variables for Astro Dev Server (npm run dev)
# Copy this file to .env and fill in your values
# .env is gitignored and will be loaded by Astro

# Note: For Cloudflare bindings (D1, R2, Durable Objects), 
# you need to use wrangler dev, not astro dev

# JWT Secret for token signing (generate with: openssl rand -base64 32)
JWT_SECRET=your-dev-jwt-secret-here-change-me

# Admin API Key for creating users (generate with: openssl rand -base64 32)
ADMIN_API_KEY=your-dev-admin-key-here-change-me

# Environment flag
ENVIRONMENT=development
